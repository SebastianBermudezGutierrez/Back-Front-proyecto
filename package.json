{
  "name": "lion-fit-pro-backend",
  "version": "1.0.0",
  "description": "Backend funcional para Lion Fit Pro (Node.js + Express + Prisma + PostgreSQL)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js",
    "migrate": "npx prisma migrate dev --name init",
    "generate": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.17.1",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "prisma": "^6.17.1"
  }
}
